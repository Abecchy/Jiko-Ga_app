<%= link_to relationship_path(current_user.active_relationships.find_by(followed: user)), id: "follow-button-for-user-#{user.id}", class: 'btn', data: { turbo_method: :delete } do %>
  <%= t('defaults.unfollow') %>
<% end %>
