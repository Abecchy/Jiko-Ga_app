<% unless current_user == @post.user %>
  <div id="follow_form">
    <% if current_user.following?(@post.user) %>
      <%= render 'users/unfollow' %>
    <% else %>
      <%= render 'users/follow' %>
    <% end %>
  </div>
<% end %>
